version: 2

models:
  - name: stg_airbnb__listing
    description: '{{ doc("model_listing") }}'

    columns:
      - name: ville
        description: La ville dans laquelle se situe le **Airbnb**.
      - name: id
        tests:
          - not_null

  - name: stg_airbnb__review
    description: ...

    columns:
    - name: listing_id
      tests:
        - not_null
